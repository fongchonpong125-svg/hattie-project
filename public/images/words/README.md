# Word Image Assets

These 62 independent `512x512` PNG files were cropped from the supplied overview image.

- They are accessed only through `wordImageMap` in `src/data.ts`.
- Quiz images contain no UI labels added by the application.
- The supplied overview image is low resolution, so these assets are usable placeholders and can be replaced later by overwriting the same filenames with higher-resolution pure images.
